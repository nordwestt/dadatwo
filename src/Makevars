PKG_LIBS += $(shell ${R_HOME}/bin/Rscript -e "RcppParallel::RcppParallelLibs()")
CXX_STD = CXX11
PKG_CPPFLAGS += -msse2
PKG_CFLAGS += -msse2
PKG_CXXFLAGS += -msse2

# AVX2 intrinsics are isolated to add_row_avx2; runtime cpuid guards execution.
taxonomy.o: CXXFLAGS += -mavx2
